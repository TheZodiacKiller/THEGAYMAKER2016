//Create sprite

//Copy previous object 

//Rename object, and edit create code with proper ID (name)

//Under initInventory(script), copy and paste one line of code and replace with proper values

//Under addToList(script), copy and paste one line of code and replace values

//Under updateInv(script), copy paste code everywhere except for when encountering specific
//eqiup slot for item and do not enter under "item that cant equip"

//Under Equip Values, under corresponding equip, copy paste "if" paragraph and replace values

//Under scr_drop(script) copy paste and replace values

//Lastly enter into corresponding "Creating Tool Tips" object and replace information4

